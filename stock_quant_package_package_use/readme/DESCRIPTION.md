This module inserts the field package_use on the stock.quant.package
views.

The package_use field is created by the stock module, but only added to
some views by the enterprise module stock_barcode_picking_batch.
